#!/usr/bin/env bash
# PMACS Utils - Disconnect VPN
# Kills OpenConnect process; vpn-slice cleans up routing automatically

set -e

# TODO: Implement
# 1. Find openconnect process
# 2. Kill it gracefully (SIGTERM)
# 3. vpn-slice handles cleanup of routes and /etc/hosts

echo "Disconnect not yet implemented. Run on macOS to develop."
